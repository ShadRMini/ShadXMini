# 📄 فهرس وسياق المشروع المعتمد (Living Memory & Project Master Index)

> **تنبيه هام (القاعدة الحديدية رقم 11 - الذاكرة الحية للمشروع):**
> هذا الملف هو **المرجع الأساسي المعتمد الوحيد (Single Source of Truth)** لمشروع **ShadXMini**. يُحدَّث إستراتيجياً وتلقائياً مع كل تعديل أو تطوير في النظام. يُحظر التعديل دون إدراج سجل السهم الإضافي في جدول التغييرات (Changelog).

---

## 1. ملخص المشروع (Project Overview)

- **اسم المشروع:** ShadXMini (سابقاً XPayStore).
- **النوع:** منصة ويب متكاملة (متجر إلكتروني للعملاء + لوحة تحكم إدارية شاملة).
- **الوصف:** منصة شادو لشحن الألعاب والتطبيقات وبرامج المحادثة الصوتية والبطاقات الرقمية وحزم الرصيد والخدمات المصرفية الرقمية.
- **المستودع الرئيسي (GitHub):** `https://github.com/ShadRMini/ShadXMini`
- **موقع المتجر (Vercel):** `https://shadxmini.vercel.app/`
- **خادم الواجهة الخلفية (Render API):** `https://shadxmini-api.onrender.com`
- **قاعدة البيانات:** Neon Console (PostgreSQL Cloud Serverless) المربوطة بـ Drizzle ORM.
- **البريد الإداري:** `shadyrahimox@gmail.com`
- **بريد الدعم الفني:** `ShadyRahimoSPT@mail.ru`
- **البنية التقنية (Tech Stack):**
  - **Monorepo Architecture (pnpm Workspaces)**
  - **Backend Server (`api-server`):** Node.js + Express.js + Drizzle ORM + PostgreSQL / Neon DB.
  - **Admin Panel (`xpay-admin`):** React + TypeScript + Vite + Tailwind CSS + Lucide Icons.
  - **Store Frontend (`xpay-store`):** React + TypeScript + Vite + Tailwind CSS + Lucide Icons.
- **الهوية البصرية الافتراضية:** طابع داكن فاخر بالتوهج الذهبي الفاخر (`#C8A45C`) مع محرك أنماط ألوان ثنائي موحد (8 أنماط جاهزة + تخصيص يدوي).

---

## 2. المنهجية والقواعد الحديدية (Methodology & Iron Rules)

### هوية الخبير الشامل (The Omnipotent Expert):
العمل بصفة مهندس برمجيات رئيسي (Lead Full-Stack Architect)، تمتلك حساً معمارياً برمجياً وشاملاً بجميع أجزاء الكود، وتعمل بدقة عالية ووعي بجميع التأثيرات المتبادلة بين الخادم واللوحة والمتجر.

### القواعد الحديدية الأحد عشر:
1. **التحليل الشامل قبل التعديل:** قراءة وفحص كل الملفات المعنية وفهم ربط البيانات بالكامل قبل كتابة أي سطر كود.
2. **الدقة والتفاصيل الدقيقة:** عدم فرض قيم وهمية أو الاستهانة بحالات الخطأ (Error Boundaries & Edge Cases).
3. **عدم التخطي أو الاختصار:** إعطاء كود عملي كامل وجاهز دون استخدام تعليقات مثل `// ... rest of code`.
4. **خارطة الطريق المحددة:** تقسيم المهام المركبة إلى مراحل منطقية سريعة ومجربة.
5. **الواقعية مع البيئة الحقيقية:** مراعاة قيود خوادم Cloud Run / Render ونظام الحاويات وتجنب الاعتماد على بيئات وهمية.
6. **الحل الجذري المباشر:** حل المشاكل من جذورها البرمجية بدلاً من الحلول الترقيعية المؤقتة.
7. **استباق المشاكل البرمجية:** حساب تبعات كل تعديل على الصفحات المرتبطة بقاعدة البيانات أو متجر العملاء.
8. **الكود الكامل المستقل:** توفير الملفات المحدثة بالكامل مع الاستيرادات والتصريح عن الأنواع (TypeScript Types).
9. **قائمة التحقق (Checklist):** تأكيد إنجاز النقاط خطوة بخطوة بعد كل بناء ناجح.
10. **التقييم الذاتي والتحقق:** تشغيل أدوات الفحص والتجميع (`compile_applet` و `pnpm run build`) فور إجراء أي تعديل.
11. **الذاكرة الحية للمشروع (Living Memory):** تحديث هذا الملف (`فهرس وسياق معتمد.md`) فورياً مع كل تعديل كمطالبة إلزامية قبل إعلان إنجاز أي مهمة.

---

## 3. خارطة الطريق العامة (General Roadmap)

- [x] **المرحلة 1:** التحويل الشامل لمتجر ويب مستقل وتشييد البنية التحتية.
- [x] **المرحلة 2:** نظام الحسابات والمصادقة الأمنية وربط قاعدة بيانات Neon.
- [x] **المرحلة 3:** الواجهات العربية RTL، الصفحات الشاملة (محفظتي، طلباتي، مفضلتي...).
- [x] **المرحلة 4:** معالجة حركات المعاملات الذرية (Transactions) لحماية الأرصدة ومنع Race Conditions.
- [x] **المرحلة 5:** لوحة التحكم الشاملة (إدارة الموردين، المنتجات، العمولات، أسعار الصرف).
- [x] **المرحلة 6:** دمج نظام الألوان المتطور (8 Presets + التبديل الديناميكي داكن/فاتح + تخصيص كامل للبطاقات والهيدر) وإنهاء الأداء النهائي (main.tsx + React.lazy).
- [x] **المرحلة 7:** القضاء الجذري على تكرار طلبات الـ API عبر Inflight Deduplication و Centralized AppDataContext (تخفيض طلبات الإقلاع من 76 إلى طلبات فردية كاش).
- [x] **المرحلة 7.5:** الأمان P0 — إغلاق 7 ثغرات حرجة (Spoofing, Admin Seeds, Secrets Fail-Closed, Orders Lock, CORS, DB Mode).
- [x] **المرحلة 7.6:** تحصين ShamCash المالي + SAM API (verify URL، atomic locks، rate limit، concurrency tests).
- [ ] **المرحلة 8 (معلقة):** الترقيم الخادمي (Server-side Pagination) للجداول الكبيرة.
- [ ] **المرحلة 9 (معلقة):** إشعارات الأحداث الحية عبر WebSockets.

---

## 4. سجل المهام المنجزة (Changelog)

| # | التاريخ والوقت | الوصف | الملفات المتأثرة | القرارات والتفاصيل |
|---|---|---|---|---|
| 1 | 2026-09-01 10:00 | تحويل XPayStore من بوت تلجرام لـ Website | `xpay-store/src/*` | بناء متجر ويب مستقل واجهات كاملة |
| 2 | 2026-09-02 11:30 | بناء نظام المصادقة بالكامل (Register/Login/Me) | `api-server/src/routes/auth.ts` | اعتماد JWT وحفظ التوكن في LocalStorage |
| 3 | 2026-09-03 14:00 | القائمة الجانبية RTL والتوجه العربي | `xpay-store/src/components/layout/Sidebar.tsx` | محاذاة وتجربة مستخدم عربية كاملة |
| 4 | 2026-09-04 16:20 | بناء صفحات مفضلتي، محفظتي، طلباتي، دفعاتي | `xpay-store/src/pages/*` | ربط مباشر بحساب المستخدم الحالي |
| 5 | 2026-09-05 09:15 | إعادة العلامة التجارية إلى ShadXMini | `metadata.json`, `index.html` | تغيير المسمى والهوية الرسمية للمتجر |
| 6 | 2026-09-06 12:45 | حماية المعاملات الذرية (Race Condition) | `api-server/src/routes/profile.ts` | استخدام SQL Transactions للخصم والإيداع |
| 7 | 2026-09-07 15:10 | إصلاح فحص توثيق الهوية (BUG-STORE-01) | `api-server/src/routes/orders.ts` | عدم السماح بالشراء إلا للعملاء الموثقين |
| 8 | 2026-09-08 11:00 | فصل حالة التوثيق عن التنبيهات | `xpay-store/src/pages/IdentityVerificationPage.tsx` | توضيح التعليمات بصرياً بصورة مستثناة |
| 9 | 2026-09-09 13:30 | إصلاح قراءة مفاتيح الموردين (BUG-SRV-03) | `api-server/src/routes/admin.ts` | تشفير وتأمين استدعاء الموردين |
| 10 | 2026-09-10 10:20 | توليد كود QR محلي بدون خدمات خارجية | `xpay-admin/src/pages/TwoFactor.tsx` | سرعة وموثوقية بالاعتماد المحلي |
| 11 | 2026-09-11 14:15 | تطبيق حد أدنى للإيداع (BUG-PKG-05) | `api-server/src/routes/deposits.ts` | منع الإيداعات دون $1.00 |
| 12 | 2026-09-12 16:50 | إدماج بوابة Binance Pay والمحافط الرقمية | `xpay-store/src/pages/DepositPage.tsx` | تسهيل خيارات الدفع والشحن المباشر |
| 13 | 2026-09-13 09:40 | تحديث وتتبع حالات الطلبات تلقائياً | `api-server/src/routes/admin.ts` | معالجة وإشعارات الحالة تلقائياً |
| 14 | 2026-09-14 11:25 | فصل تخصيص شحن الرصيد والتحكم الكامل | `xpay-admin/src/pages/DepositSettings.tsx` | إدارة طرق الشحن والتعليمات والروابط |
| 15 | 2026-09-14 15:00 | زر وصفحة إدارة CronJobs للمهام المجدولة | `xpay-admin/src/pages/CronJobs.tsx` | إمكانية التشغيل اليدوي للوظائف الفورية |
| 16 | 2026-09-15 10:10 | حفظ وتوثيق صلاحيات أدمن الإدارة Permissions | `xpay-admin/src/pages/Permissions.tsx` | بناء نظام حماية الأدوار والمسارات |
| 17 | 2026-09-15 13:45 | إصلاح QR Code في المصادقة الثنائية 2FA | `xpay-admin/src/pages/TwoFactor.tsx` | رسم الشفرة بنجاح واستقرار باللوحة |
| 18 | 2026-09-15 17:00 | دمج Currencies + CurrencySettings | `xpay-admin/src/pages/CurrencySettings.tsx` | توحيد شاشة تحويل أسعار الصرف |
| 19 | 2026-09-16 09:30 | مزامنة العملة والتحويل بالمتجر | `xpay-store/src/lib/currency.ts` | عرض المبالغ بالدولار والليرة السورية |
| 20 | 2026-09-16 12:15 | دمج Banners الإعلانات والعروض الترويجية | `xpay-admin/src/pages/Banners.tsx` | إدارة البنرات واللافتات في مكان واحد |
| 21 | 2026-09-16 15:40 | حفظ وتتبع `provider_order_id` في الطلبات | `api-server/src/routes/admin.ts` | منع ضياع رقم الطلب لدى المورد |
| 22 | 2026-09-16 18:20 | إصلاح معادلة الخسارة المالية في تسعير المنتجات | `api-server/src/routes/orders.ts` | تصحيح حساب هامش الربح والعمولة |
| 23 | 2026-09-17 08:30 | تقسيم حزم اللوحة الإدارية عبر React.lazy | `xpay-admin/src/App.tsx` | تقليل حجم الملفات عند التحميل الأولي |
| 24 | 2026-09-17 10:15 | تبويبات التحكم بالصفحات (Auth/Product/About...) | `xpay-admin/src/pages/ThemeNew.tsx` | تقسيم وتحسين تحكم التصميم شمولياً |
| 25 | 2026-09-17 12:00 | تنظيف `!important` واستبدالها بـ CSS Vars | `xpay-store/src/lib/theme.ts` | إتاحة التجاوب والسلاسة في الثيمات |
| 26 | 2026-09-17 14:00 | إعادة هيكلة الألوان (8 أنماط + المعاينة) | `xpay-admin/src/pages/ThemeNew.tsx` | إضافة أنماط جاهزة وتخصيص كامل |
| 27 | 2026-09-17 15:00 | إصلاح خطأ `bgSecondary is not defined` | `xpay-store/src/lib/theme.ts` | تعريف المتغير المفقود وتأكيد البناء |
| 28 | 2026-09-17 15:10 | إنشاء وثيقة الذاكرة الحية (Living Memory) | `/فهرس وسياق معتمد.md` | تطبيق القاعدة الحديدية رقم 11 بالكامل |
| 29 | 2026-09-18 | Phase 0 - تشخيص شامل لـ 3 مشاكل رئيسية | `Sidebar.tsx`, `theme.ts`, `index.css`, `DepositPageUI.tsx`, `DepositShamCash.tsx`, `DepositPayPage.tsx`, `ProductCard.tsx`, `CategoryCard.tsx` | اكتشاف 3 أسباب جذرية + 11 ملفاً متأثراً |
| 30 | 2026-09-18 | Phase 1 - إصلاح زر Dark/Light + استبدال الألوان الثابتة | `Sidebar.tsx`, `index.css`, `theme.ts`, `ProductCard.tsx`, `CategoryCard.tsx`, `AnnouncementBar.tsx`, `NotificationBellDropdown.tsx`, `PopupNotification.tsx` | مزامنة Sidebar مع getStoreThemeMode + إزالة !important + تغطية درجات الشفافية |
| 31 | 2026-09-18 | Phase 1.1 - إصلاح جذري لخطأ ReferenceError: setStoreThemeMode | `Sidebar.tsx`, `theme.ts` | إضافة استيراد setStoreThemeMode و applyStoreTheme المفقودة في Sidebar.tsx + توحيد مفاتيح localStorage إلى theme_mode |
| 32 | 2026-09-18 | Phase 1.2 - إصلاح جذري لتبديل الوضع (Dark/Light Mode) | `xpay-store/src/lib/theme.ts` | معالجة دقيقة ومستقلة لمتغيرات الوضع الفاتح (bgPrimary, bgCard, textPrimary, bgInput, etc.) وإزالة احتكار ألوان الخادم الداكنة في نمط Light Mode + Bounding data-theme="light" |
| 33 | 2026-09-18 | Phase 1.3 - إصلاح اسم المستخدم في الرئيسية للوضع الفاتح | `artifacts/xpay-store/src/pages/home.tsx` | تحويل لون اسم المستخدم والشارات المرافقة إلى متغيرات متجاوبة ديناميكياً (var(--theme-text-primary)) دون مساس بالبطاقة المالية أو الملفات المحظورة |
| 34 | 2026-09-18 | Phase 2A — إصلاح النصوص غير المرئية + إزالة QR وWallet الثابتين | `DepositPageUI.tsx`, `DepositShamCash.tsx`, `DepositPayPage.tsx`, `index.css` | استبدال الألوان الثابتة بـ var(--theme-*) + إزالة القيم الافتراضية الثابتة والـ QR الثابت بـ QR ديناميكي |
| 44 | 2026-09-18 | Phase 3E.1 — إصلاح: page variables يجب أن تُحذف، لا تُعيَّن | `theme.ts`, `meta.ts`, `index.css` | حذف المتغيرات عبر removeProperty وتصفيتها في meta.ts عند غياب التخصيص لتفعيل Fallback Chain تلقائياً |
| 45 | 2026-09-18 | Phase 3E.2 — منع القيم الافتراضية في لوحة التحكم من كسر Fallback | `SectionThemeControls.tsx`, `AboutSettings.tsx`, `ContactSettings.tsx`, `AuthPagesSettings.tsx`, `ProductPageSettings.tsx`, `meta.ts`, `theme.ts` | تصفري الحقول، إضافة زر مسح لكل حقل، تحديث قائمة القيم المرفوضة DEFAULT_VALUES_TO_REJECT لتفعيل Fallback Chain تلقائياً |
| 46 | 2026-09-19 | Phase 3G — إصلاح النصوص غير المرئية والألوان الذهبية المتبقية | `IdentityVerification.tsx`, `ContactPage.tsx`, `meta.ts`, `ensureSchema.ts` | إزالة الألوان الثابتة وتفعيل سلسلة Fallback الشاملة (var(--page-*, var(--theme-*))) مع تنظيف قاعدة البيانات والتوافق التام مع Light Mode والأنماط الثمانية |
| 47 | 2026-09-20 | Phase 6 — إنهاء الأداء النهائي: main.tsx + React.lazy | `main.tsx`, `App.tsx` | تخطي انتظار Telegram للمتصفحات العادية وتطبيق React.lazy لكافة صفحات المتجر |
| 48 | 2026-09-20 | Phase 7 — القضاء الجذري على تكرار طلبات API | `api-client.ts`, `AppDataContext.tsx`, `custom-fetch.ts`, `public-api.ts`, `auth-context.tsx`, `App.tsx`, `Sidebar.tsx`, `PopupNotification.tsx`, `AnnouncementBar.tsx`, `NotificationBellDropdown.tsx`, `BannerCarousel.tsx`, `theme.ts`, `favorites.tsx`, `product-detail.tsx`, `about.tsx`, `Login.tsx`, `Register.tsx` | بناء Inflight Deduplication + 30s TTL Caching + مزامنة مركزية للبيانات العامة عبر AppDataContext مما خفض طلبات الإقلاع المتكررة من 76 إلى طلبات فردية كاش بنسبة انخفاض تجاوزت 70% |
| 53 | 2026-09-21 | P0-1: منع انتحال x-telegram-id | `currentUser.ts`, `main.tsx` | حظر استخدام ترويسة x-telegram-id غير المصادق عليها واعتماد التحقق الصارم عبر التوكن فقط لمنع تزوير الهويات |
| 54 | 2026-09-21 | P0-2: ADMIN_SEED_* من env | `seedAdmin.ts`, `.env.example`, `render.yaml` | قراءة بيانات الأدمن الأولي من متغيرات البيئة بدلاً من أي قيم هاردكودد مع تحديث أمثلة البيئة وملف النشر |
| 55 | 2026-09-21 | P0-3: requireAdmin على notifications/meta | `notifications.ts`, `meta.ts` | فرض وسيط الحماية requireAdmin على مسارات الإشعارات والإعدادات الإدارية لمنع الوصول غير المصرح |
| 56 | 2026-09-21 | P0-4: Webhook secrets fail-closed + ENABLE_TELEGRAM_WEBHOOKS | `app.ts`, `telegram-admin.ts`, `telegram-store.ts` | تفعيل استراتيجية Fail-Closed الصارمة لأسرار الـ Webhooks وإضافة مفتاح بيئة للتحكم بتفعيل الخطافات |
| 57 | 2026-09-21 | P0-5: Orders Race Condition | `orders.ts` | حماية طلبات الشراء عبر معاملات قاعدة البيانات الذرية SELECT ... FOR UPDATE على رصيد المستخدم ومخزون المنتجات |
| 58 | 2026-09-21 | P0-6: CORS مقيد بـ CLIENT_URL | `app.ts` | تقييد مشاركة الموارد عبر الأصول CORS لتقتصر على نطاق العميل الموثوق المصرح به فقط |
| 59 | 2026-09-21 | P0-7: رفض JSON fallback في production | `lib/db/src/index.ts` | إلغاء التراجع إلى التخزين المحلي JSON عند التشغيل في الإنتاج وفرض الاتصال بـ PostgreSQL / Neon DB حصراً |
| 60 | 2026-09-21 | getCurrentUserOptional + rateLimit + إصلاح unread-count | `currentUser.ts`, `notifications.ts` | ضبط الاستعلام عن هوية المستخدم الاختيارية وتطبيق حدود المعدل وإصلاح استعلام عداد الإشعارات غير المقروءة |
| 61 | 2026-09-21 | تصحيح SAM verify URL: /api/pay/{id}/verify | `deposits.ts` | تصحيح مسار التحقق من فواتير شام كاش ليتطابق مع التوثيق الرسمي لـ SAM API |
| 62 | 2026-09-21 | تصحيح SAM status URL: /api/pay/{id} | `deposits.ts` | تصحيح مسار الاستعلام عن حالة الفاتورة ليتطابق بدقة مع SAM API Docs |
| 63 | 2026-09-21 | إضافة expires_at + pending_review إلى deposits | `schema/index.ts`, `ensureSchema.ts`, `deposits.ts` | إضافة عمودي الصلاحية الزمنية والتدقيق الإداري المعلق مع هجرات SQL التلقائية |
| 64 | 2026-09-21 | verify_rate_limits table + unique index | `schema/index.ts`, `ensureSchema.ts` | إنشاء جدول وحدود محاولات التحقق لمنع هجمات التخمين مع قيود فريدة على (user_id, deposit_id) و (ip, deposit_id) |
| 65 | 2026-09-21 | idx_deposits_approved_tx_ref (partial unique) | `schema/index.ts`, `ensureSchema.ts` | إنشاء فهرس فريد جزئي للمعاملات المعتمدة لمنع تكرار رقم الحوالة transaction_ref بين الإيداعات |
| 66 | 2026-09-21 | إعادة ترتيب approveShamCashDepositAtomic (throw بدل return، إزالة ON CONFLICT) | `deposits.ts` | تحويل آلية اعتماد الإيداعات لمعاملة ذرية مغلقة بنظام SELECT ... FOR UPDATE ورمي أخطاء التزامن لمنع الصرف المزدوج |
| 67 | 2026-09-21 | findIncomingShamCashTransactionByRef — 5 فحوصات | `deposits.ts` | إضافة فحوصات الصرامة الخمسة (type=credit, currency, amount >= expected, occurredAt >= created, occurredAt <= expires) |
| 68 | 2026-09-21 | verifyShamCashPayment موحّد + Early approved check | `deposits.ts` | توحيد منطق التحقق وفحص حالة الاعتماد المسبق مبكراً لتجنب إرجاع أخطاء 400 على المعاملات المعتمدة متزامناً |
| 69 | 2026-09-21 | Webhook secret timingSafeEqual + fail-closed | `deposits.ts` | تأمين مقارنة توقيع الويب هوك ضد هجمات التوقيت عبر crypto.timingSafeEqual مع رفض فوري عند غياب السر |
| 70 | 2026-09-21 | Backoff + Circuit Breaker لـ SAM API + SHAMCASH_SYNC_ENABLED | `deposits.ts` | إضافة قاطع الدائرة والتراجع الأسي عند اضطراب المزود الخارجي ومفتاح لتعطيل المزامنة عند الحاجة |
| 71 | 2026-09-21 | P0-1: إزالة القيمة الافتراضية لسر JWT (Fail-Closed) وتأمين التوكنات | `currentUser.ts`, `app.ts`, `.env.example`, `render.yaml` | حذف القيمة الافتراضية الهاردكودد لـ JWT_SECRET نهائياً وتطبيق نموذج Fail-Closed الصارم عند بدء التشغيل مع رفض التوكنات الموقعة بالمفتاح القديم |
| 72 | 2026-09-21 | P0-1b: إصلاح انحدار وضع الزائر + توحيد isAuthenticated / isGuest | `auth-context.tsx`, `AppLayout.tsx`, `Sidebar.tsx`, `home.tsx`, `CategoryCard.tsx`, `ProductCard.tsx`, `BannerCarousel.tsx` | بناء دالة التحقق isValidUserObject وتوحيد مصدر الحقيقة لحالة الزائر والمستخدم المسجل ومنع تلوث الجلسة عبر الكاش التالف أو معرّف الزائر الافتراضي |
| 73 | 2026-09-24 | تطبيق Rate Limiting على مسارات المصادقة العامة (/auth/login و /auth/register) | `artifacts/api-server/src/routes/auth.ts` | تطبيق وسيط rateLimit على تسجيل الدخول (10 محاولات / 15 دقيقة) وإنشاء الحساب (5 محاولات / 15 دقيقة) لمنع هجمات التخمين Brute Force والحسابات الوهمية |
| 74 | 2026-09-24 | إضافة عمود provider_order_uuid وحماية المتجر من ثغرة فقدان order_uuid والـ Timeout | `lib/db/src/schema/index.ts`, `api-server/src/lib/ensureSchema.ts`, `api-server/src/routes/orders.ts` | إضافة عمود وفهرس provider_order_uuid صريح في قاعدة البيانات وجدول ordersTable، حفظ المعرّف قبل الاتصال بالمزود، منع الاسترجاع الخاطئ للأموال عند انقطاع الاتصال أو الـ Timeout بجعل الحالة wait وتأكيد الإنجاز بـ 3 اختبارات آلية ناجحة |
| 75 | 2026-09-24 | تفعيل Background Worker لمزامنة الطلبات المعلقة + Cooldown لمدة 3 دقائق و Escalation بعد 24 ساعة | `api-server/src/routes/orders.ts`, `api-server/src/app.ts`, `api-server/src/routes/admin.ts`, `.env.example`, `render.yaml` | بناء دالة المزامنة الشاملة syncAllPendingProviderOrders، تشغيل عامل خلفية آمن دوري (5 دقائق افتراضياً) مع حماية ضد التكرار، منع الفحص المتكرر Cooldown (3 دقائق) وتصعيد الطلبات العالقة بعد 24 ساعة واستبدال Stub مهام الـ Cron الإدارية |

---

## 5. المهام المعلقة (Pending Tasks)

1. **الترقيم الخادمي (Server-side Pagination):** للجداول الكبيرة لتقليل حجم الاستجابة.
2. **التصدير الموحد للتقارير (Excel / CSV Export):** لتقارير الأرباح والمبيعات والإيداعات.
3. **تنبيهات WebSockets الحية:** تحديث حالة الطلبات تلقائياً فورياً بدون إعادة التحميل.

---

## 6. الملفات الحساسة والمهمة (Critical Files)

- **`artifacts/api-server/src/routes/deposits.ts`**: يحتوي الدوال المالية الحرجة `approveShamCashDepositAtomic`, `verifyShamCashPayment`, `findIncomingShamCashTransactionByRef`.
- **`lib/db/src/schema/index.ts` & `artifacts/api-server/src/lib/ensureSchema.ts`**: تعريف ومزامنة جداول قاعدة البيانات والقيود الفريدة مثل `shamcash_used_transaction_refs`, `verify_rate_limits`, `deposits`.
- **`artifacts/api-server/src/lib/ensureSchema.ts`**: يضمن إنشاء إعدادات قاعدة البيانات الافتراضية والـ DDL Migrations.
- **`artifacts/api-server/src/routes/admin.ts`**: تحتوي كافة مسارات الإدارة و CRUD للأنماط المخصصة.
- **`artifacts/api-server/src/routes/meta.ts`**: تزود المتجر بكافة متغيرات الثيم والإعدادات العامة عبر `/api/public/theme-settings`.
- **`artifacts/xpay-admin/src/pages/ThemeNew.tsx`**: الواجهة الرئيسية لإدارة وتعديل نظام الألوان والأنماط التسعة.
- **`artifacts/xpay-admin/src/lib/theme-presets.ts`**: موديول تعريف الـ 8 أنماط الجاهزة بالوضعين الفاتح والداكن.
- **`artifacts/xpay-admin/src/components/ThemePresetCard.tsx`**: بطاقات عرض واختيار الأنماط الجاهزة.
- **`artifacts/xpay-admin/src/components/ColorPickerField.tsx`**: أداة انطباع واختيار الألوان الموحدة.
- **`artifacts/xpay-store/src/lib/theme.ts`**: المحرك المسؤول عن حقن وتحديث متغيرات CSS Variables وخطوط Google Fonts في المتجر.
- **`artifacts/xpay-store/src/components/layout/AppLayout.tsx`**: التخطيط الرئيسي لصفحات المتجر.

---

## 7. مفاتيح قاعدة البيانات الحالية (Database Keys in `settings`)

### مفاتيح الألوان والمظهر (`theme_*`):
- `theme_mode`: `"dark"`
- `theme_active_preset`: `"gold"`
- `theme_primary`: `"#C8A45C"`
- `theme_secondary`: `"#B8954A"`
- `theme_accent`: `"#FDE68A"`
- `theme_background`: `"#1A1A1A"`
- `theme_card`: `"#2D2D2D"`
- `theme_text_primary`: `"#FFFFFF"`
- `theme_text_secondary`: `"#E5E7EB"`
- `theme_text_muted`: `"#9CA3AF"`
- `theme_border`: `"rgba(200, 164, 92, 0.25)"`
- `theme_input_bg`: `"#3D3D3D"`
- `theme_header_gradient_start`: `"#1A1A1A"`
- `theme_header_gradient_end`: `"#2D2D2D"`
- `theme_bottom_nav`: `"#1A1A1A"`
- `theme_bottom_nav_active`: `"#C8A45C"`
- `theme_sidebar_bg`: `"#1A1A1A"`
- `theme_font_arabic`: `"Cairo"`
- `theme_font_english`: `"Inter"`
- `theme_font_size`: `"14"`
- `theme_heading_size`: `"20"`
- `theme_border_radius`: `"16"`
- `theme_shadow`: `"medium"`
- `theme_padding`: `"24"`
- `theme_logo_size`: `"80px"`
- `theme_logo_url`: `""`
- `theme_logo_text_color`: `"#C8A45C"`
- `theme_presets`: `[]` (مصفوفة الأنماط المخصصة الإضافية)

### مفاتيح بطاقة الرصيد والمحفظة (`balance_*`):
- `balance_gradient_start`: `"#1E40AF"`
- `balance_gradient_mid`: `"#3B82F6"`
- `balance_gradient_end`: `"#60A5FA"`
- `balance_text_color`: `"#FFFFFF"`
- `balance_subtext_color`: `"#E0F2FE"`
- `balance_badge_color`: `"#FFFFFF"`
- `balance_currency_color`: `"#FFFFFF"`
- `balance_radius`: `"24"`
- `balance_padding`: `"24"`
- `balance_amount_size`: `"32"`
- `balance_label_size`: `"14"`
- `balance_shadow`: `"heavy"`
- `balance_glow_enabled`: `"true"`
- `balance_glow_color`: `"#3B82F6"`

### المفاتيح العامة والخدمية:
- `store_name`: `"ShadMini"`
- `store_description`: `"متجر شادو لشحن الألعاب والتطبيقات وبرامج الشات والخدمات الرقمية."`
- `admin_email`: `"shadyrahimox@gmail.com"`
- `support_email`: `"ShadyRahimoSPT@mail.ru"`
- `shamcash_api_base_url`: `"https://www.sam-api.pro/api"`
- `shamcash_invoice_expiry_minutes`: `"15"`

---

## 8. الأنماط الـ 8 (Theme Presets)

| ID | الاسم العربي | English Name | Primary (Dark / Light) | Accent (Dark / Light) | Background (Dark / Light) | Card (Dark / Light) |
|---|---|---|---|---|---|---|
| `gold` | الذهبي الفاخر | Luxury Gold | `#C8A45C` / `#C8A45C` | `#FDE68A` / `#D97706` | `#1A1A1A` / `#F5F2EB` | `#2D2D2D` / `#FFFFFF` |
| `royal-blue` | الأزرق الملكي | Royal Blue | `#3B82F6` / `#3B82F6` | `#93C5FD` / `#1D4ED8` | `#0F172A` / `#F1F5F9` | `#1E293B` / `#FFFFFF` |
| `imperial-emerald` | الزمردي الإمبراطوري | Imperial Emerald | `#10B981` / `#10B981` | `#6EE7B7` / `#047857` | `#064E3B` / `#ECFDF5` | `#065F46` / `#FFFFFF` |
| `classic-violet` | البنفسجي الكلاسيكي | Classic Violet | `#8B5CF6` / `#8B5CF6` | `#C4B5FD` / `#6D28D9` | `#2E1065` / `#F5F3FF` | `#3B0764` / `#FFFFFF` |
| `ruby-pink` | الوردي الياقوتي | Ruby Pink | `#EC4899` / `#EC4899` | `#F9A8D4` / `#BE185D` | `#4C0519` / `#FDF2F8` | `#831843` / `#FFFFFF` |
| `fire-red` | الأحمر الناري | Fire Red | `#EF4444` / `#EF4444` | `#FCA5A5` / `#B91C1C` | `#450A0A` / `#FEF2F2` | `#7F1D1D` / `#FFFFFF` |
| `calm-indigo` | النيلي الهادئ | Calm Indigo | `#4F46E5` / `#4F46E5` | `#A5B4FC` / `#3730A3` | `#1E1B4B` / `#EEF2FF` | `#312E81` / `#FFFFFF` |
| `modern-teal` | التركوازي العصري | Modern Teal | `#14B8A6` / `#14B8A6` | `#99F6E4` / `#0F766E` | `#042F2E` / `#F0FDFA` | `#115E59` / `#FFFFFF` |

**معرف النمط النشط حالياً:** `gold`

---

## 9. روابط النشر والبيئة (Deployment Links)

- **GitHub Repository:** `https://github.com/ShadRMini/ShadXMini`
- **Vercel Storefront:** `https://shadxmini.vercel.app/`
- **Render Backend API:** `https://shadxmini-api.onrender.com`
- **Neon Cloud Console:** PostgreSQL Serverless Database Instance.

---

## 10. المشاكل المعلقة والحلول (Pending/Resolved Issues)

### المشاكل المعلقة الفعلية:
- 🟡 **تناسق الأنماط الـ 8**: دعم باقي الصفحات ذات الألوان الثابتة المتفرقة.

### المشاكل التي تم حلها سابقاً:
- **[محلول - 21 سبتمبر 2026] حزمة التحصين المالي والأمني P0 + SAM API + منع التزامن المزدوج (Race Condition):**
  1. **الموقع والملفات**: `deposits.ts`, `currentUser.ts`, `seedAdmin.ts`, `notifications.ts`, `orders.ts`, `app.ts`, `schema/index.ts`, `ensureSchema.ts`, `lib/db/src/index.ts`.
  2. **القرارات والحلول**:
     - إغلاق 7 ثغرات أمنية حرجة P0 (منع انتحال الهويات عبر `x-telegram-id`، إزالة أي حسابات أدمن هاردكودد، حماية مسارات الإشعارات بـ `requireAdmin`، تقييد CORS بـ `CLIENT_URL`، منع التراجع إلى JSON في Production، وقفل عمليات شراء الطلبات ذرياً بـ `SELECT ... FOR UPDATE`).
     - تصحيح مسارات استدعاء SAM API وفق التوثيق الرسمي (`/api/pay/{id}/verify` و `/api/pay/{id}`).
     - تحويل دالة `approveShamCashDepositAtomic` بالكامل لمعاملة ذرية موحدة تعتمد على `SELECT ... FOR UPDATE` مع إزالة `ON CONFLICT DO UPDATE` لمنع خطأ زيادة الرصيد المزدوج.
     - إضافة جدول وسجلات الفحص `shamcash_used_transaction_refs` كـ Unique Ledger مع الفهرس الجزئي الفريد `idx_deposits_approved_tx_ref`.
     - تطبيق جدول وحدود محاولات التحقق `verify_rate_limits` لمنع هجمات التخمين العنيف (5 محاولات لكل دقيقة).
     - إضافة الفحوصات الخمسة الصارمة داخل دالة `findIncomingShamCashTransactionByRef` وتمرير كائن `deposit` كاملاً لضمان مطابقة النوع والعملة والمبلغ والنافذة الزمنية (15 دقيقة مع هامش 5 دقائق).
     - التحقق الفعلي من نجاح سيناريوهات التزامن الحقيقي (`bash -c 'curl & curl & wait'`) بنجاح تام وإضافة الرصيد لمرة واحدة فقط.
- **[محلول - Phase 3C / المهمة 41] إصلاح جذري: Light Mode يقرأ من النمط النشط:**
  1. **الموقع والملفات**: `theme.ts`, `meta.ts`, `theme-presets.ts`.
  2. **القرارات والحلول**:
     - إزالة كافة القيم الـ hardcoded للوضع الفاتح (`#F5F2EB`, `#EFECE6`, `rgba(0,0,0,0.12)`, إلخ) من `theme.ts`.
     - إنشاء `artifacts/xpay-store/src/lib/theme-presets.ts` يحتوي على التعريفات الكاملة للأنماط الـ 8 بوضعيها الداكن والفاتح.
     - تحديث نقطة النهاية API `/api/public/theme-settings` في `meta.ts` لإعادة كائن النمط النشط `activePreset` وكائنات `dark` و `light` المخصصة لكل نمط.
     - تعديل `applyStoreTheme` بقراءة جميع متغيرات المظهر المضيء (`--theme-background`, `--theme-card`, `--theme-input-bg`, `--theme-border`, `--theme-text-*`, `--theme-header-gradient`, `--theme-bottom-nav`, `--theme-sidebar-bg`) ديناميكياً من `activePreset[mode]`.
- **[محلول - Phase 2E / المهمة 38] تحسين سرعة عرض البيانات (Skeleton + Cache + Preload):**
  1. **الموقع والملفات**: `DepositShamCash.tsx`, `DepositPageUI.tsx`, `shamcash-cache.ts`, `theme.ts`, `App.tsx`.
  2. **القرارات والحلول**:
     - إضافة Loading Skeleton أنيق وبأبعاد متطابقة في `DepositPageUI.tsx` يظهر بدلاً من الفراغ أثناء جلب البيانات.
     - إنشاء طبقة التخزين المؤقت `shamcash-cache.ts` مع استراتيجية Stale-While-Revalidate عبر `localStorage` بـ TTL 5 دقائق.
     - إضافة Preload مبكر لبيانات ShamCash في `App.tsx` عند إقلاع التطبيق لجاهزية البيانات الفورية (0ms).
     - تسريع تحميل الثيم `theme-settings` في `theme.ts` عبر التخزين المؤقت في `localStorage` لتطبيق المظهر بـ 0ms.
- **[محلول - Phase 2D / المهمة 37] إصلاح Modal التكبير (Lightbox) للـ QR عند غياب walletAddress:**
  1. **الموقع**: تم تحديد جميع المكونات المسؤولة عن Modal التكبير في `DepositShamCash.tsx` و `DepositPageUI.tsx` بالإضافة إلى `DepositPayPage.tsx` و `shamcash-invoice-verify.tsx`.
  2. **المنطق**: تم تعديل منطق الفتح والعرض بحيث يُمنع فتح Modal التكبير عند غياب `walletAddress` و `qrImageUrl` مع إلغاء حالة المؤشر `cursor-pointer` وإخفاء تلميح "تكبير الرمز".
  3. **الوقاية الحصينة**: في حال فتح الـ Modal لأي سبب، يتم فحص وجود المحفظة وفي حال عدم وجودها يتم عرض التنبيه القياسي "لا توجد محفظة مُعدّة - يرجى التواصل مع الدعم" بدلاً من إظهار أي QR وهمي أو هاردكودد.
- **[محلول - Phase 2B / المهمة 35] التوليد والتكوين الديناميكي لعنوان محفظة ShamCash والـ QR Code:**
  1. استبدال القيم الوهمية الهاردكودد (`SHAM-CASH-PAY` و `35147b...`) بنصوص واجهة احترافية عند غياب المحفظة ("يرجى التواصل مع الدعم" / "يرجى ضبط المحفظة من لوحة التحكم").
  2. دعم الشرط الديناميكي لإظهار/إخفاء الـ QR Code عند غياب عنوان المحفظة.
  3. تحديث بذرة قاعدة البيانات `ensureSchema.ts` لتصبح المحفظة فارغة `""` افتراضياً للمنشآت الجديدة.
  4. إصلاح الألوان وانعكاس البطاقة وتصميم QR بمتغيرات CSS الخاصة بـ ShamCash (`--shamcash-*`).
  5. إنشاء تبويب ShamCash في لوحة التحكم (Admin Panel) تحت تخصيص المظهر بـ 12 حقل مع معاينة حية (Live Preview).
  6. بناء نقاط النهاية API العامة والإدارية `/api/admin/shamcash-settings` و `/api/public/shamcash-settings`.
  7. ربط متغيرات شام كاش الفلويد بمحرك الثيمات `theme.ts`.
- **[محلول - Phase 7.7 / المهمة 72] إصلاح انحدار وضع الزائر (Guest Mode) وتوحيد مصدر الحقيقة لحالة المصادقة:**
  1. **المشكلة الجذرية:** كان `auth-context.tsx` يعتبر أي قيمة مخزنة في كاش `localStorage` (أو كائنات الزوار الافتراضية مثل `{ id: "0", identityMissing: true }`) كمستخدم مسجل بسبب التحقق `isAuthenticated = Boolean(user || token)`، مما يكسر وضع الزائر ويظهر بطاقة الرصيد وعناصر المستخدم المسجل للزوار.
  2. **الحل والتنفيذ:**
     - بناء دالة تحقق صارمة `isValidUserObject(u)` ترفض أي كائن بدون معرّف حقيقي صالح أو يحمل `id: "0"` أو `identityMissing: true`.
     - تنظيف الكاش وحالة الـ state تلقائياً في `auth-context.tsx` عند بدء التشغيل أو عند استلام 401 من `/api/me`.
     - توحيد حساب وتصدير `isAuthenticated = isValidUserObject(user)` و `isGuest = !loading && !isAuthenticated`.
     - إزالة الحسابات المحلية لـ `isGuest` و `isAuthenticated` من كافة مكونات المتجر (`AppLayout.tsx`, `Sidebar.tsx`, `home.tsx`, `CategoryCard.tsx`, `ProductCard.tsx`, `BannerCarousel.tsx`) والاعتماد الحصري على `useAuth()`.
     - الحفاظ على سلوك `ProtectedRoute.tsx` الصارم المعتمد على `isAuthenticated` فقط.

- **[محلول - Phase 7.8 / المهمة 74] إصلاح ثغرة فقدان order_uuid في الشراء وحماية المتجر من الخسائر المالية:**
  1. **المشكلة الجذرية:** كان `POST /orders` ينشئ `randomUUID()` بشكل عشوائي ومؤقت داخل استدعاء `placeOrder` دون حفظه مسبقاً في سجل الطلب بقاعدة البيانات (`meta.orderUuid`). عند حدوث انقطاع اتصال أو Timeout من طرف المزود (60 ثانية)، كان الـ Exception يُلتقط ويُعتبر الطلب `rejected` فوراً ويتم استرجاع الرصيد للمستخدم بينما المزود قد يكون نفّذ الشحن فعلياً، مما يسبب خسارة مزدوجة للمتجر ولا يتيح تتبع أو فحص الطلب لاحقاً.
  2. **الحل والتنفيذ:**
     - توليد `orderUuid` بشكل دائم ومسبق وتخزينه في `baseMeta.orderUuid` و `baseMeta.provider.orderUuid` وقاعدة البيانات ضمن المعاملة الذرية لخصم الرصيد.
     - تمرير `orderUuid` الموثق إلى محول المزود `placeOrder`.
     - تحصين معالجة الأخطاء والـ Timeout: عند فشل الاتصال أو حدوث مهلة زمنية، يتم إبقاء حالة الطلب معلقة `wait` بدلاً من الرفض والاسترجاع الفوري، مع محاولة تحقق فورية عبر `checkProviderOrderImmediately` باستخدام `orderUuid` (مع تفعيل `byUuid: true`).
     - تحديث محرك المزامنة `syncPendingProviderOrdersForUser` ليدعم الاستعلام والتحقق عبر `orderUuid` وتحديث `providerOrderId` وحالة الطلب بدقة.
     - حفظ وتحديث `providerOrderId` الصريح في عمود `ordersTable.providerOrderId` وفي الميتاداتا.

- **[محلول - Phase 2A] إصلاح ألوان حقول مدخلات الإيداع وزر تأكيد الإيداع في ShamCash.**
- **[محلول - Phase 1] زر Dark/Light في Sidebar:** تم إصلاح مزامنة حالة `isDark` ومستمعات الأحداث + إزالة `!important` من `index.css` وتغطية درجات الشفافية ديناميكياً واستبدال الألوان الثابتة في المكونات الـ 5.
- **[محلول] خطأ `bgSecondary is not defined`:** تم تعريف المتغير بدقة في `xpay-store/src/lib/theme.ts`.
- **[محلول] التعارض في `!important`:** تم تنظيف الأنماط واعتماد المتغيرات الفلويد عبر CSS Custom Properties.
- **[محلول] تفقد معرّف طلب المورد `provider_order_id`:** تم الحفظ الصريح في جدول الطلبات ومنع فقدانه أثناء التحديث.

---

## 11. أسلوب التواصل والتفضيلات (Communication Style)

- **أسلوب المستخدم:** يفضل الوضوح، العمل المباشر، المعاينة الحية، والتنفيذ الكامل مع الالتزام التام بالقواعد بدون أي اختصارات أو تجديد غير مبرر للرموز.
- **التصميم:** داكن فاخر بالتوهج الذهبي (`#C8A45C`)، دعم كامل للغة العربية والاتجاه من اليمين لليسار RTL.

---

## 12. ملاحظات معمارية وتاريخ القرارات (Architecture & Decisions)

1. **التحكم الديناميكي التلقائي في الألوان:** جميع متغيرات CSS يتم حشوهـا مباشرة في الـ DOM عند تغيير إعدادات الهوية بدون الحاجة لإعادة تحميل كامل المتجر.
2. **استقلالية المنصة (Monorepo Isolation):** الفصل التام بين مسارات الخادم والمستودعات تجنباً للتعارض أثناء عمليات النشر التلقائي على Vercel و Render.

- **[محلول - Phase 3E.3 / المهمة 46] الإصلاح الجذري لتسلسل Fallback Chain وإزالة الألوان الافتراضية الثابتة للملفات:**
  1. **الموقع والملفات**: `artifacts/xpay-store/src/lib/theme.ts`, `artifacts/api-server/src/routes/meta.ts`, `artifacts/xpay-store/src/index.css`.
  2. **القرارات والحلول**:
     - تمرير كافة متغيرات الأقسام والصفحات (`auth_*`, `product_*`, `about_*`, `contact_*`) عبر دالة `setOrRemoveProp` التي تفحص قيم الألوان وتتأكد من إزالة الخاصية من الـ DOM في حال كانت فارغة أو قيمة افتراضية هاردكودد.
     - معالجة خطوط الأقسام المخصصة (`*_font_family`) بنفس المنطق عبر `setOrRemoveProp` وتوسيع قائمة `DEFAULT_VALUES_TO_REJECT` لتشمل قيم خطوط وصياغات الألوان الافتراضية.
     - التأكد من خلو `index.css` تماماً من أي تعيينات ثابتة لمتغيرات الصفحات في `:root`.
     - التأكد من أن مسار الـ API `/api/public/theme-settings` في `meta.ts` يعيد قِيماً فارغة `""` لصفحات الأقسام غير المخصصة عبر `getPageCustomSetting` لضمان عمل الـ Fallback Chain بدقة.

- **[محلول - Phase 6 / المهمة 51] إنهاء الأداء النهائي: تحسين main.tsx وتقسيم كود المتجر الشامل بـ React.lazy:**
  1. **إصلاح `main.tsx`:** تم التحقق من بيئة التشغيل بحيث يتم فحص `isTelegramWebApp` فقط. للمتصفحات العادية، يتم تخطي انتظار معرف تيليجرام (`waitForTelegramIdentity`) وتسجيل الجلسة بالكامل، مما وفر 1.5 ثانية كاملة عند التحميل الأول.
  2. **تطبيق `React.lazy` في `App.tsx`:** تحويل كافة استيرادات صفحات ومسارات المتجر (`Home`, `ProductDetail`, `Categories`, `WalletPage`, `Orders`, `Deposit`, إلخ) إلى استيرادات كسولة ديناميكية مع الحفاظ على `AppLayout`, `ProtectedRoute`, و `PageLoading` كاستيرادات ثابتة، وتغليف موجه المسارات `Router` بـ `<Suspense fallback={<PageLoading />}>`.
  3. **النتائج الحسابية للحزم:** تم تقليص حجم الحزمة الرئيسية من 1,714.92 KB إلى 657.10 KB غير مضغوط، ومن 414 KB إلى 190.74 KB بعد ضغط Gzip (تخفيض بنسبة 53.9% لحجم Gzip و 61.7% للحجم الأولي)، مع توزيع باقي الشاشات على 66 Chunk مستقل يُحمَّل عند الطلب فقط.

- **[محلول - Phase 7 / المهمة 52] القضاء الجذري على تكرار طلبات API في المتجر (Deduplication & Caching):**
  1. **المشكلة:** عند فتح الصفحة الرئيسية كان المتجر يُطلق أكثر من 76 طلباً مع تكرار هائل لنقاط النهاية (`/api/me` 6+ مرات، `/api/me/notifications/unread-count` 8+ مرات، `/api/public/*` 5+ مرات، `/api/categories` 3+ مرات، `/api/banners` 3+ مرات، إلخ) مع استهلاك باندويث 636 KB وإبطاء LCP إلى 2.64s.
  2. **الحل المعماري:**
     - بناء محرك `apiFetch` في `api-client.ts` مزود بـ Inflight Deduplication (دمج الطلبات المتزامنة لنفس الـ Endpoint في Promise واحد) و TTL Memory Cache لمدة 30 ثانية للطلبات المتتالية.
     - بناء سياق البيانات الموحد `AppDataContext.tsx` الذي يجلب بيانات الفئات، البنرات، الأخبار، الإعلانات، وإعدادات البوب آب دفعة واحدة بالتوازي وبشكل مركزي ويوفرها عبر `useAppData()` لكافة مكونات المتجر.
     - جدولة جلب عداد الإشعارات `/api/me/notifications/unread-count` في استطلاع مركزي منظم (Polling) كل 60 ثانية بدلاً من إطلاقه العشوائي من كل مكون ومستمع.
     - تحديث طبقة `custom-fetch.ts` في حزمة `@workspace/api-client-react` لتدعم Inflight Deduplication والـ Caching التلقائي لضمان استفادة أي Hook عام من محرك إزالة التكرار.
     - توجيه مكتبة `public-api.ts` ومزود المصادقة `auth-context.tsx` ومحرك الثيمات `theme.ts` ومكونات المتجر (`Sidebar`, `PopupNotification`, `AnnouncementBar`, `NotificationBellDropdown`, `BannerCarousel`, `Login`, `Register`, `about`, `favorites`, `product-detail`) لاستخدام `apiFetch` أو `useAppData`.
     - تأمين مسح الكاش التلقائي `clearApiCache()` عند تسجيل الدخول أو الخروج أو إجراء أي عملية تعديل (Mutation).
  3. **النتيجة المحققة:** انخفاض عدد الطلبات المتزامنة والمتكررة على الشبكة بنسبة تفوق 70%، توفير استهلاك البيانات للزوار، تسريع استجابة الواجهة LCP و FCP واستقرار كامل دون كسر أي وظيفة دفع أو حسابات أو أرصدة.

---

**آخر تحديث:** 24 سبتمبر 2026 - المهمة 74: إصلاح ثغرة فقدان order_uuid في الشراء وحماية المتجر من الخسائر المالية الناجمة عن Timeout المزود واسترجاع الأموال الخاطئ.
