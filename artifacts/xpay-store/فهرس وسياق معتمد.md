# فهرس وسياق العمليات والتعديلات المعتمدة

## المرحلتان المتنقلتان مؤخراً:
- **Phase 3G**: إصلاح نصوص وبطاقات توثيق الهوية (Identity Verification) والزر الذهبي المتبقي.
- **Phase 3H**: الحل الجذري النهائي لبطاقات الأسئلة الشائعة (FAQ) وعناصر النماذج وشارة الرصيد.
  - تم تحديث شارة الرصيد في `AppLayout.tsx` و `Sidebar.tsx` لتأخذ اللون الأساسي المتباين `var(--theme-primary)`.
  - تم تطهير كافة البطاقات وحقول الإدخال في `ContactPage.tsx` (سواء في النمط التقليدي Legacy Mode أو النمط الديناميكي Dynamic Mode) واستبدال الألوان الثابتة الداكنة بمتغيرات الثيم مثل `var(--theme-card)` و `var(--theme-input-bg)` و `var(--theme-border)` و `var(--theme-text-primary)` مع سلاسل Fallback سليمة100%.
